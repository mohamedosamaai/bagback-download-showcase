{
  "name": "bagback-download-showcase",
  "version": "0.1.0",
  "private": true,
  "description": "Bagback Download — Public Architecture, Interface Contracts & SDK Showcase",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npx tsc --project packages/core && npx tsc --project packages/downloader-engine"
  },
  "license": "MIT"
}
